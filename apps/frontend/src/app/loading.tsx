export default function Loading() {
  // This is a placeholder for the global loading skeleton.
  // In Next.js App Router, loading.tsx automatically wraps page components with React Suspense.
  // It provides instant feedback to users while the actual page content is being fetched.
  return null;
}
